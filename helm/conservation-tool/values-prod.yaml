environment:
  id: "prod"
  name: "prod"

# Memory requests: 1536Mi for running services + 512Mi for deployment hooks = 2Gi.
# The quota applies to requests. Running service limits total 2Gi, with 1088Mi for the worker.
prefect-server:
  ingress:
    host:
      hostname: "conservation-tool-prefect-server-fa9440-prod.apps.gold.devops.gov.bc.ca"
  ui:
    podSecurityContext:
      runAsUser: 1003590000
      fsGroup: null
    containerSecurityContext:
      runAsUser: 1003590000
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

  server:
    uiConfig:
      prefectUiApiUrl: "https://conservation-tool-prefect-server-fa9440-prod.apps.gold.devops.gov.bc.ca/api"
    resources:
      requests:
        cpu: 50m
        memory: 384Mi
      limits:
        cpu: 100m
        memory: 384Mi
    podSecurityContext:
      runAsUser: 1003590000
      fsGroup: null
    containerSecurityContext:
      runAsUser: 1003590000
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

  migrations:
    securityContext:
      runAsUser: 1003590000
      runAsNonRoot: true
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      capabilities: {}

database:
  postgresql:
    primary:
      podSecurityContext:
        runAsUser: 1003590000
        fsGroup: 1003590000
      containerSecurityContext:
        runAsUser: 1003590000
        runAsNonRoot: true
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: false
        capabilities:
          drop:
            - ALL

services:
  frontend:
    resources:
      requests:
        memory: 64Mi
      limits:
        memory: 64Mi
    image:
      tag: "prod"
    securityContext:
      runAsUser: 1003590000
      fsGroup: 1003590000

  workflows:
    worker:
      image:
        tag: "prod"
      securityContext:
        runAsUser: 1003590000
        fsGroup: 1003590000
      profiles:
        sparse-solver:
          executionProfile: prod-1088mi
          # Leave 320Mi of the pod limit for Prefect and orchestration overhead.
          daskWorkerMemory: 768MiB
          maxPeakMemoryBytes: 805306368
          resources:
            requests:
              cpu: 100m
              memory: 576Mi
            limits:
              cpu: "2"
              memory: 1088Mi

    deploy:
      image:
        tag: "prod"
      securityContext:
        runAsUser: 1003590000
        fsGroup: 1003590000

  api:
    resources:
      requests:
        memory: 256Mi
      limits:
        memory: 256Mi
    image:
      tag: "prod"
    route:
      host: "conservation-tool-api-fa9440-prod.apps.gold.devops.gov.bc.ca"
    securityContext:
      runAsUser: 1003590000
      fsGroup: 1003590000

  db:
    resources:
      requests:
        memory: 256Mi
      limits:
        memory: 256Mi
    image:
      tag: "prod"
    securityContext:
      runAsUser: 1003590000
      fsGroup: 1003590000

  dbSetup:
    image:
      tag: "prod"

backgroundServices:
  podSecurityContext:
    runAsUser: 1003590000
    fsGroup: 1003590000
  containerSecurityContext:
    runAsUser: 1003590000
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL

env:
  NODE_OPTIONS: "--max-old-space-size=160"
  NODE_ENV: "production"
  FRONTEND_API_HOST: "https://conservation-tool-api-fa9440-prod.apps.gold.devops.gov.bc.ca"
