environment:
  id: "test"
  name: "test"

# Memory limits: 1536Mi for running services + 512Mi for deployment hooks = 2Gi.
# Prefect receives 1088Mi of the running-service budget (704Mi worker + 384Mi server).
prefect-server:
  ingress:
    host:
      hostname: "conservation-tool-prefect-server-fa9440-test.apps.gold.devops.gov.bc.ca"
  ui:
    podSecurityContext:
      runAsUser: 1003620000
      fsGroup: null
    containerSecurityContext:
      runAsUser: 1003620000
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

  server:
    uiConfig:
      prefectUiApiUrl: "https://conservation-tool-prefect-server-fa9440-test.apps.gold.devops.gov.bc.ca/api"
    resources:
      requests:
        cpu: 50m
        memory: 384Mi
      limits:
        cpu: 100m
        memory: 384Mi
    podSecurityContext:
      runAsUser: 1003620000
      fsGroup: null
    containerSecurityContext:
      runAsUser: 1003620000
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

  migrations:
    securityContext:
      runAsUser: 1003620000
      runAsNonRoot: true
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      capabilities: {}

database:
  postgresql:
    primary:
      podSecurityContext:
        runAsUser: 1003620000
        fsGroup: 1003620000
      containerSecurityContext:
        runAsUser: 1003620000
        runAsNonRoot: true
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: false
        capabilities:
          drop:
            - ALL

services:
  frontend:
    resources:
      requests:
        memory: 64Mi
      limits:
        memory: 64Mi
    image:
      tag: "test"
    securityContext:
      runAsUser: 1003620000
      fsGroup: 1003620000

  workflows:
    worker:
      image:
        tag: "test"
      securityContext:
        runAsUser: 1003620000
        fsGroup: 1003620000
      profiles:
        sparse-solver:
          executionProfile: test-704mi
          # Leave 320Mi of the pod limit for Prefect and orchestration overhead.
          daskWorkerMemory: 384MiB
          maxPeakMemoryBytes: 402653184
          resources:
            requests:
              cpu: 100m
              memory: 704Mi
            limits:
              cpu: "2"
              memory: 704Mi

    deploy:
      image:
        tag: "test"
      securityContext:
        runAsUser: 1003620000
        fsGroup: 1003620000

  api:
    resources:
      requests:
        memory: 128Mi
      limits:
        memory: 128Mi
    image:
      tag: "test"
    route:
      host: "conservation-tool-api-fa9440-test.apps.gold.devops.gov.bc.ca"
    securityContext:
      runAsUser: 1003620000
      fsGroup: 1003620000

  db:
    resources:
      requests:
        memory: 256Mi
      limits:
        memory: 256Mi
    image:
      tag: "test"
    securityContext:
      runAsUser: 1003620000
      fsGroup: 1003620000

  dbSetup:
    image:
      tag: "test"

backgroundServices:
  podSecurityContext:
    runAsUser: 1003620000
    fsGroup: 1003620000
  containerSecurityContext:
    runAsUser: 1003620000
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
        - ALL

env:
  NODE_OPTIONS: "--max-old-space-size=64"
  NODE_ENV: "test"
  FRONTEND_API_HOST: "https://conservation-tool-api-fa9440-test.apps.gold.devops.gov.bc.ca"
