apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: {{ include "web-app.fullname.prefect-server" . }}
  namespace: {{ .Values.namespace }}
spec:
  runPolicy: Serial
  source:
    type: Git
    git:
      uri: {{ .Values.gitRepo }}
      ref: {{ .Values.gitRef | default "main" }}
    contextDir: workflows
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile.server
  output:
    to:
      kind: ImageStreamTag
      name: {{ .Values.services.workflows.server.image.stream }}:{{ .Values.services.workflows.server.image.tag }}
  triggers:
    - type: ConfigChange
    - type: ImageChange
  successfulBuildsHistoryLimit: 3
  failedBuildsHistoryLimit: 1
