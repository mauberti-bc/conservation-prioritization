apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: {{ include "web-app.fullname.prefect-deploy" . }}
  namespace: {{ .Values.namespace }}
spec:
  runPolicy: Serial
  source:
    type: Git
    git:
      uri: {{ .Values.gitRepo }}
      ref: {{ .Values.gitRef | default "main" }}
    contextDir: workflows
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile.deploy
  output:
    to:
      kind: ImageStreamTag
      name: {{ .Values.services.workflows.deploy.image.stream }}:{{ .Values.services.workflows.deploy.image.tag }}
  triggers:
    - type: ConfigChange
    - type: ImageChange
  successfulBuildsHistoryLimit: 3
  failedBuildsHistoryLimit: 1
