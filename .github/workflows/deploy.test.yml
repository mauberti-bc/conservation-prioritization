name: Promote to Test

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Confirm deployment"
        required: true
        type: boolean
        default: false

jobs:
  deploy:
    # only proceed if the user checked the confirmation box
    if: ${{ inputs.confirm }}
    uses: ./.github/workflows/deploy.yml
    with:
      source_tag: dev
      target_tag: test
      environment: test
