name: Promote to Prod

on:
  release:
    types: [published]

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      source_tag: test
      target_tag: prod
      environment: prod
