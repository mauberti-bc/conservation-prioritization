name: Promote to Prod

on:
  release:
    types: [published]

concurrency:
  group: promote-to-prod
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      source_tag: test
      target_tag: prod
      image_tag: prod
      environment: prod